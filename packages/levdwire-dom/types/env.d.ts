/**
 * Indicates whether the current environment is development.
 *
 * Used to conditionally enable logging, warnings, or debug-only behavior.
 * Typically replaced at build time by bundlers like Vite, Webpack, or esbuild.
 */
declare const __DEV__: boolean
